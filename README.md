# Sounds Good - 音标学习应用

一个帮助学习英式和美式音标的 Web 应用。

## 📁 项目结构

```
sounds-good/
├── src/              # 源代码
│   ├── components/   # React 组件
│   ├── hooks/        # 自定义 Hooks
│   ├── contexts/     # React Context
│   ├── constants/    # 常量定义
│   ├── types/        # TypeScript 类型
│   ├── utils/        # 工具函数
│   ├── main.tsx      # 应用入口
│   └── index.css     # 全局样式
├── public/           # 静态资源（音频文件等）
├── data/             # 数据文件
├── index.html        # HTML 入口
└── ...配置文件
```

## 🚀 开始使用

### 安装依赖

```bash
pnpm install
```

### 开发模式

```bash
pnpm dev
```

访问 [http://localhost:3001](http://localhost:3001) 查看应用。

### 构建生产版本

```bash
pnpm build
```

构建产物将生成在 `dist` 目录。

### 预览生产构建

```bash
pnpm preview
```

## 🔧 技术栈

- **构建工具**: Vite 6
- **框架**: React 19
- **样式**: Tailwind CSS
- **语言**: TypeScript
- **包管理器**: pnpm

## 📝 脚本说明

- `pnpm dev` - 启动 Vite 开发服务器（端口 3001）
- `pnpm build` - 类型检查 + 构建生产版本
- `pnpm preview` - 预览生产构建
- `pnpm check-types` - 仅类型检查
- `pnpm split-audio` - 分割音频文件工具

## 📄 License

MIT

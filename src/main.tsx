import React from 'react'
import ReactDOM from 'react-dom/client'
import { SoundEasyApp } from './sound-easy-app'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
        <SoundEasyApp />
    </React.StrictMode>,
)
